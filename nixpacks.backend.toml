# Nixpacks configuration for Railway backend service
# This overrides auto-detection to use requirements.backend.txt

[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r requirements.backend.txt"]

[phases.build]
cmds = ["echo 'Backend dependencies installed'"]

[start]
cmd = "python api_server.py"
